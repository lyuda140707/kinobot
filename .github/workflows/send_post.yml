# .github/workflows/send_post.yml
name: 🚀 Щоденний пост у канал

on:
  schedule:
    - cron: '0 12 * * *'  # Запускається щодня о 12:00 UTC
  workflow_dispatch:

jobs:
  send_post:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python 3.x
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install python-telegram-bot

      - name: Run send_channel_post.py
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHANNEL_ID: ${{ secrets.CHANNEL_ID }}  # ваш CHANNEL_ID, наприклад @YourChannel
        run: python send_channel_post.py
```

```python
# send_channel_post.py
import os
from telegram import Bot

def main():
    token = os.getenv('BOT_TOKEN')
    channel = os.getenv('CHANNEL_ID')

    bot = Bot(token=token)

    text = (
        "🎬 Привіт! Саме час поділитися нашими улюбленими каналами:
"
        "
"  "— @KinoTochkaFilms
"
        "— @KinoTochkaUA
"
        "
Не забувайте підписуватися, щоб бути в курсі всіх новинок! 🍿"
    )

    bot.send_message(chat_id=channel, text=text)

if __name__ == '__main__':
    main()
